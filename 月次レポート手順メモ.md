# 家計レポート 運用メモ（PDF一本化・恩株はP3）

家計の可視化は **PDFレポート `create_report.py` に一本化**しました。
以前の別枠HTMLダッシュボード（monthly_report / onkabu_dashboard / toushin_us_dashboard 等）は
**役目を終えたため `archive/` へ退避**しています。今後の更新・確認はこのPDFだけを見ればOKです。

- **成果物**：`create_report.py` を実行 → A4・**7ページのPDF**を生成 → **最新PDFをGmailへ自動送信**
- **恩株（高配当株の元本回収）は P3** に統合済み（配当だけで取得元本を何%取り戻したか）
- ページ構成：P1 ダッシュボード（前月比）／P2 NISA・DC／**P3 恩株**／P4 不動産・老後／P5 分析サマリー／P6 保険／P7 世帯年収

---

## 唯一の正（single source of truth）

**Googleドライブ `I:\マイドライブ\20_claude code\90_個人\94_家計管理\create_report.py` が正本です。**
複数PC・複数の作業場所で別々に編集すると分岐します（実際に恩株版v18と家計改善版v24が分岐しました）。
そのため次のルールを徹底します：

1. **編集する前に、必ずDriveの最新 `create_report.py` を土台にする**（GitHubリポジトリは私の作業＆バックアップ用ミラー）。
2. 変更したら、そのファイルを Drive の同じ場所に**上書き保存**する。
3. 恩株ページ（P3）は毎回残す。P3を落とした版を作らない。

---

## 毎月の更新（月初・所要5分）

1. **CSVをダウンロード**して `00_マスタデータ置き場\06_家計_資産データ\資産推移月次\` に置く
   - マネーフォワード ME：資産推移月次CSV（`資産推移月次_YYYYMMDD.csv`。P1の内訳6分類の列名が必要）
   - 必要に応じ SBI証券の保有証券一覧・配当履歴（恩株の数値更新用。下記参照）
2. PowerShellで実行（1行）：
   ```
   python "I:\マイドライブ\20_claude code\90_個人\94_家計管理\create_report.py"
   ```
3. `OK … vNN.pdf ＋ メール送信完了` と出れば、**最新PDFがGmail（ph2144.tt.0609@gmail.com）に届く**
   （送信結果は `メール自動送信_仕組みと送信ログ.md` に自動追記）

> アプリパスワードは環境変数 `KAKEI_MAIL_PASS`（コード直書きしない）。未設定ならPDF生成のみでスキップ。
> 手動実行時に読み込むには：
> `$env:KAKEI_MAIL_PASS = [Environment]::GetEnvironmentVariable('KAKEI_MAIL_PASS','User')`

---

## 恩株（P3）の数値を更新する

恩株の数値は `create_report.py` 冒頭の **`ONK_` 定数ブロック**（`# ==== 恩株データ` の直後・現状 280行目あたり）に集約しています。
SBIの最新値が出たら、この定数を差し替えるだけでP3が更新されます。

| 定数 | 意味 |
|---|---|
| `ONK_COST` | 取得額(簿価)・夫婦日本株＋SPYD＝恩株の分母 |
| `ONK_DIV` | 生涯 受取配当(税引後・保有銘柄)＝分子 |
| `ONK_TTM` | 年間配当ペース（直近1年・税引後） |
| `ONK_LIFE_TOTAL` | 受取配当 生涯累計（国内＋米国・売却分含む） |
| `ONK_BY_YEAR` | 年別 受取配当（税引後）の推移グラフ |
| `ONK_RANK` | 銘柄別ランキング：(銘柄, 所有者, 口座, 取得単価, 株数, 取得額, 配当累計, 恩株%) |
| `ONK_ACCT` | 口座別サマリー：(口座, 取得額, 恩株%, 直近1年配当) |
| `ONK_NEAR` / `ONK_REM_Y` / `ONK_MORE` | ハイライト文・残り年数・その他銘柄の注記 |

`ONK_PCT`（恩株達成度）は `ONK_DIV ÷ ONK_COST` で自動計算されます。

- 対象範囲：日本株（夫婦合算）＋SPYD（米国高配当ETF）。SPCX・ボーイングは無配の成長株なので恩株には含めない。
- 恩株の「保有年数／残り年数」は配当の初回受取日と現配当維持を前提とした目安。

---

## リマインド
- Googleカレンダーに「毎月1日 9:00：📊 先月のマネーレポート作成」を登録済み（繰り返し）
